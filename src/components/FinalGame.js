const FinalGame = (props) => {
  return (
    <>
      <button
        className="buttons__playAgain"
        type="button"
        // onClick={props.handleSkip}
      >
        Play Again
      </button>
    </>
  );
};

export default FinalGame;

const FinalGame = () => {
  return (
    <>
      <button
        className="playAgain__btn"
        type="button"
        // onClick={props.handleSkip}
      >
        Play Again
      </button>
    </>
  );
};

export default FinalGame;
